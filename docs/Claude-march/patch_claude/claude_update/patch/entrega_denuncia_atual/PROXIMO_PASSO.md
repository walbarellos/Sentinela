# Proximo Passo

Com base no que ja temos, o proximo passo mais forte nao e genérico. E um trilho dedicado para `NORTE`.

## Passo 0. Fechar a revisao conservadora de vinculo societario

Status atual:
- a camada `vinculo_politico_societario` ja avaliou `37` empresas com `QSA + contratos`
- apareceu `1` caso com sobreposicao exata:
  `CEDIMP` (`13.325.100/0001-30`)
  `R$ 6.604.510,00` no contrato `779/2023` da `SESACRE`
  `2` socios aparecendo nominalmente como servidores concursados da `SEMSA`
- o follow-up de saude agora confirmou mais um fato:
  os mesmos `2` nomes aparecem no modulo oficial de profissionais do `CNES` da propria `CEDIMP`
  ambos com `CBO 225320 - MEDICO EM RADIOLOGIA E DIAGNOSTICO POR IMAGEM`
  ambos com `20h` ambulatoriais no estabelecimento privado
- o follow-up agora ficou mais forte:
  a ficha individual oficial desses mesmos profissionais no `CNES` materializa `131` competencias com concomitancia documental entre linha `ESTATUTARIO / SERVIDOR PROPRIO` e linha da `CEDIMP`
- a camada agora tambem ficou quantificada:
  `2` profissionais com concomitancia
  `262` competencias concomitantes no total
  `234` competencias `>=60h`
  `28` competencias `>=80h`
  pico documental de `100h`
- o sistema classificou isso corretamente como:
  `HIPOTESE_INVESTIGATIVA / EXPLORATORIO / REVISAO_INTERNA` para a camada societaria basica
  `HIPOTESE_INVESTIGATIVA / INDICIARIO / REVISAO_INTERNA` para o follow-up de saude
  `FATO_DOCUMENTAL / DOCUMENTAL_CORROBORADO / APTO_APURACAO` para a coincidencia nominal na ficha oficial de profissionais do CNES
  `FATO_DOCUMENTAL / DOCUMENTAL_PRIMARIO / APTO_APURACAO` para a concomitancia documental no historico oficial do CNES
  `FATO_DOCUMENTAL / DOCUMENTAL_PRIMARIO / APTO_APURACAO` para a carga concomitante documentada no historico oficial do CNES

Entrega esperada:
- confirmar se a concomitancia documental do CNES revela incompatibilidade juridica real ou apenas acumulacao licita
- usar os `CNS` oficiais ja capturados para reduzir a dependencia do CPF mascarado do QSA
- confrontar a metrica ja calculada por competencia com a regra juridica aplicavel, sem transformar `>=60h` ou `>=80h` em veredito automatico
- checar regime, lotacao, jornada e compatibilidade funcional dos dois medicos a luz da norma aplicavel
- se houver base suficiente, elevar de `sobreposicao societaria exata` para `potencial conflito de interesse`; se nao houver, manter como triagem interna e nao como representacao

## Passo 1. Fechar os blocos AGRO fora do eixo principal da SEJUSP

Status atual:
o projeto agora ja separa tres frentes diferentes da `AGRO`:
- `NORTE-CENTRO / SEJUSP` = terceirizacao/apoio operacional com mao de obra, contrato atual `076/2024`, `R$ 1.215.990,24`, sem `id_licitacao` exposto no portal, mas ja ligado no DOE a `ARP 04/2024`, `PP 053/2023` e ao processo `0819.014451.00277/2024-18`;
- `AGRO NORTE / SEJUSP` = `6` contratos de viaturas/caminhonetes, `R$ 2.987.976,00`, sem `id_licitacao` exposto no card do portal, porem com atos formais ja fechados para `004/2023`, `151/2023`, `082/2024`, `147/2024`, `149/2024`, adesoes `33/2024` e `69/2024`, e agora tambem o `170/2023` via `DJE/TJAC 24/10/2023`, `ARP 304/2022`, `PE SRP 74/2022` e DOE `08/12/2023`.
- `AGRO / DETRAN` = `R$ 2.716.895,28` em `5` contratos
  aquisicao principal `022/2023` (`R$ 2.296.950,00`) sem `id_licitacao` exposto no portal
  manutencoes/revisoes posteriores `001/2023`, `071/2023`, `007/2024` e `086/2024` no mesmo CNPJ
  DOE do `022/2023` ja fecha `6` viaturas a `R$ 382.825,00` por unidade
  auditoria formal ja aponta compatibilidade quantitativa com o `071/2023`, que referencia `6` veiculos L200
- `AGRO / execucao penal` = `R$ 4.068.000,00`
  `038/2023` / `FUNPENACRE` / `R$ 1.330.000,00`
    agora fechado como contrato exato no DOE, com `5` caminhonetes a `R$ 266.000,00` por unidade
    consistente com o `Termo de Adesao 5/2023/IAPEN` no mesmo processo `4005.014135.00006/2023-90`
  `073/2023` / `IAPEN` / `R$ 254.000,00`
    agora fechado como contrato exato no DOE de `04/08/2023`
    termo de adesao `26/2023/IAPEN`, processo `4005.014141.00047/2023-70`, `PE SRP 258/2022`, `ARP 010/2022-SEPA`
    card estadual `id_contrato 82564` convergente com o mesmo objeto da `CIAP / Convenio 905916/2020 MJ/DEPEN`
  `072/2024` / `ISE` / `R$ 2.484.000,00`
  todos ainda sem `id_licitacao` exposto no portal
  DOE do `072/2024` ja fecha `10` caminhonetes por `R$ 2.480.000,00`
  portal x DOE do `072/2024` divergem hoje em `R$ 4.000,00`

Tambem ja ha linha do tempo publica congelada para `SEJUSP`:
- `2020` NORTE-CENTRO / PE SRP `023/2019` / apoio administrativo-logistica-operacional / `R$ 2.069.261,04`
- `2023` JWC / PE SRP `241/2021` / apoio operacional-administrativo com dedicacao exclusiva
- `2023` AGRO NORTE / contratos `004/2023` e `151/2023` / `ARP 008/2022` / `PE 318/2022 - SEPA`
- `2024` NORTE-CENTRO / contrato `076/2024` / `ARP 04/2024` / `PP 053/2023`
- `2024` AGRO NORTE / contratos `082/2024`, `147/2024`, `149/2024` / `ARP 49/2023` / `PE 206/2023` / `FNSP`
- `2025` NORTE-CENTRO / contrato `26/2021` / limpeza com mao de obra

Tambem ja existe uma auditoria objetiva `portal x DOE`:
- `004/2023` = consistente
- `151/2023` = divergente
  portal = `10` caminhonetes
  DOE = `1` caminhonete para o `CIEPS`

Entrega esperada:
- manter `038/2023` e `073/2023` como blocos formalmente resolvidos;
- manter `022/2023` do `DETRAN` e `072/2024` do `ISE` como blocos ja suficientemente fechados para sustentar o pacote atual;
- manter o `SEJUSP` como bloco documentalmente fechado para `2023/2024`, com `170/2023` ja resolvido por adesao autorizada do `TJAC`.

## Passo 2. Fechar o que ainda falta no bloco SEJUSP

Status atual:
esse ciclo ja ficou suficientemente separado em dois eixos:
- `NORTE-CENTRO / SEJUSP` = terceirizacao/apoio operacional com mao de obra, contrato atual `076/2024`, `R$ 1.215.990,24`, sem `id_licitacao` exposto no portal, mas ja ligado no DOE a `ARP 04/2024`, `PP 053/2023` e ao processo `0819.014451.00277/2024-18`;
- `AGRO NORTE / SEJUSP` = `6` contratos de viaturas/caminhonetes, `R$ 2.987.976,00`, sem `id_licitacao` exposto no card do portal, porem com atos formais ja fechados para `004/2023`, `151/2023`, `082/2024`, `147/2024`, `149/2024`, adesoes `33/2024` e `69/2024`, e o `170/2023` agora ligado a `ARP 304/2022` e `PE SRP 74/2022 - TJAC`.

Tambem ja existe uma auditoria objetiva `portal x DOE`:
- `004/2023` = consistente
- `151/2023` = divergente
  portal = `10` caminhonetes
  DOE = `1` caminhonete para o `CIEPS`

Entrega esperada:
- aprofundar `DETRAN 022/2023` e `ISE 072/2024`, que agora concentram o maior valor remanescente da trilha `AGRO`;
- consolidar a linha do tempo `SEJUSP` por processo, ARP, pregão e fonte de recurso;
- usar o `170/2023` como caso-modelo de fechamento por fonte externa primaria (`TJAC + DOE`), replicando o metodo nos demais blocos com maior valor.

## Passo 3. Consolidar a trilha exclusiva NORTE com base em vinculo exato

Status atual:
esse passo base ja foi concluido. O software agora tem:
- `trace_norte` materializado com contratos municipal + estadual, sancoes CEIS, QSA e checagem nominal exata em bases locais;
- `3` empresas-lead de terceirizacao com nome semelhante enriquecidas com QSA;
- `0` compartilhamentos exatos de socio entre esses leads e a `NORTE` focal.
- `47` vinculos exatos `contrato -> id_licitacao -> processo` materializados a partir do portal estadual.

Entrega esperada:
- auditar primeiro o bloco `CENTRAL NORTE / SEPLAN` ja resolvido para `Pregao Presencial 053/2023`, processo `0088.016739.00008/2023-72`
- congelar edital, termo de referencia, quadro de postos, resultado/homologacao e anexos desse processo
- aprofundar a divergencia do `PP 053/2023`: DOE homologa `NORTE COMERCIO E SERVICOS (21.813.150/0001-94)` e os contratos aparecem em nome de `CENTRAL NORTE (36.990.588/0001-15)`
- revisar os vinculos ainda sem processo resolvido no detalhe de licitacao, principalmente:
  `AGRO NORTE / DETRAN` (`R$ 2.716.895,28`)
  `AGRO NORTE / ISE` (`R$ 2.484.000,00`)
  `AGRO NORTE / FUNPENACRE` (`R$ 1.330.000,00`)
  `NORTE - CENTRO / FEM` (`R$ 853.658,04`)
- so depois reabrir a busca por grupo economico ou interpostas pessoas

## Passo 4. Separar fornecimento de bens x terceirizacao de pessoal

Hoje o que esta provado para este CNPJ da `NORTE` no banco sao contratos de fornecimento de bens.

Para sustentar a linha que voce relatou sobre substituicao de servidores efetivos, precisamos achar contratos com objeto parecido com:

- locacao de mao de obra
- apoio administrativo
- servicos terceirizados
- fornecimento de pessoal
- servicos continuados
- apoio operacional

Se esses objetos nao aparecerem nesse CNPJ, o software deve procurar outro CNPJ da mesma rede empresarial.

## Passo 5. Cruzar socio e rede politica

Com o QSA basico em maos, o passo seguinte e cruzar:

- socios e administradores
- ocupantes de cargo em comissao
- agentes politicos
- parentescos ou nomes recorrentes em nomeacoes

Esse e o ponto em que nepotismo deixa de ser relato e pode virar trilha objetiva.

## Passo 6. Fechar o eixo juridico

No que ja esta pronto hoje, os casos mais imediatos para representacao sao:

- `3895` no municipal
- `3898` no municipal
- top `10` sancionado da `SESACRE`

O passo seguinte para `NORTE` nao e protocolar mais uma representacao generica. E montar uma representacao especifica da empresa, com:

- historico contratual no Acre
- historico sancionatorio
- QSA
- objetos contratados
- eventual rede politica ligada aos socios

## Leitura franca

Hoje ja temos prova suficiente para investigar `NORTE` com seriedade.
Hoje ainda nao temos prova suficiente para afirmar, so com o banco atual, nepotismo e fraude licitatoria estrutural dessa empresa no Acre.

O melhor proximo passo agora e aprofundar os blocos `AGRO / DETRAN` e `AGRO / ISE`, porque o `170/2023` da `SEJUSP/PMAC` saiu da fila de pendencia.
O `073/2023` do `IAPEN` e o `170/2023` da `SEJUSP` deixaram de ser pendencias: ambos agora estao amarrados a fonte primaria suficiente para trilha probatoria.
No eixo de vinculo societario/saude, o melhor proximo passo deixou de ser raspagem basica e passou a ser compatibilidade juridico-funcional: CPF completo, jornada, acumulacao licita e vinculo profissional no ecossistema CNES.
Com a ficha individual oficial do `CNES` ja amarrada, o proximo ciclo correto e transformar a concomitancia historica em metrica documental de carga por competencia e depois confrontar isso com a regra juridica aplicavel.
Essa parte de metrica documental ja foi implementada. O ciclo seguinte agora e juridico-funcional: regime, norma, jornada e eventual compatibilidade real.
