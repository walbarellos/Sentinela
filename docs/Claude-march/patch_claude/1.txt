Agora tenho tudo. Escrevendo com o schema exato.Vai em `scripts/sync_ceis_cnep.py`. Como você já tem os ZIPs em `data/federal/`:

```bash
python scripts/sync_ceis_cnep.py --data 20260312
```

O que foi corrigido em relação à versão anterior:
- URL `YYYYMMDD` (não `YYYYMM`)
- Schema canônico real: `id`, `kind`, `severity`, `confidence`, `exposure_brl`, `title`, `description_md`, `pattern`, `sources` (JSON), `tags` (JSON), `sample_n`, `unit_total` + campos institucionais
- `kind = SESACRE_SANCAO_CEIS` / `SESACRE_SANCAO_CNEP` — compatível com o `SANCAO_KIND_PREFIX` que já existe no `sync_sesacre_sancoes.py`
- JOIN usa `orgao_canonico` (não `orgao`) que é o campo real na `estado_ac_pagamentos`
- ZIPs resolvidos por prioridade: `--local-*` → cache em `data/federal/YYYYMMDD_*.zip` → `--download`
